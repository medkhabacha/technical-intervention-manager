# Technical Intervention Manager

A lightweight web application built with Python, Flask, and SQLite to manage technical interventions. It features role-based access control, allowing administrators to create and assign tasks, and technicians to update the status of their assigned tasks.

## ⚙️ Prerequisites

Before you begin, ensure you have Python installed on your system. You will also need `pip` (Python's package installer).

## 🚀 Setup & Installation

1. **Save the application file:**
   Ensure the Python script is saved as `app.py` in your project folder.

2. **Install the required dependencies:**
   Open your terminal or command prompt, navigate to your project folder, and run the following command:
   ```bash
   pip install Flask Flask-SQLAlchemy